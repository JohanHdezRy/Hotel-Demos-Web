/* ── Types for demo-4 (El Fenn) ── */

export interface FeatureItem {
  img: string;
  label: string;
}

export interface RoomItem {
  img: string;
  name: string;
  price: string;
}

export interface ActivityItem {
  img: string;
  label: string;
  sub: string;
}

export interface BlogItem {
  img: string;
  title: string;
  excerpt: string;
}

